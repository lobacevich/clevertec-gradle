plugins {
    id 'java'
}

allprojects {
    group = 'by.clevertec.lobacevich'
    version = '1.0'

    repositories {
        mavenCentral()
    }
}

jar {
    manifest {
        attributes('name': project.name, 'version': version)
    }
}
